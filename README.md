# vision inference benchmark


GOAL: take a vision inference model through pytorch baseline through ONNX runtime, TensorRT FP16 and INT8, measuring throghput latency (p50, p95, p99), accuracy and VRAM at each stage.

## status
-- TO BE UPDATED 

--under construction --

